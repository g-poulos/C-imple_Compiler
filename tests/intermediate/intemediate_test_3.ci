program max
{    var a,b,c,d,e;
     function max(in x, in y)
     {   if (x>y)
            return(x);
         else
            return(y);
     }
     e:=max(in max(in a, in b),
        in max(in c, in d));
}.