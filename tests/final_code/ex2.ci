program main
{
    declare a,b,A,B;
    function f1(in a, inout b)
    {
        function f2()
        {
            B:=A;
            b:=a;
        }
        A:=2;
        f2();

    }
    a:=3;
    A:=4;
    f1(in a, inout A);
    print(A);
    print(B);
}